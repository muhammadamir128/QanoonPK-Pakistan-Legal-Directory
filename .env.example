# Database connection string (SQLite file or PostgreSQL URL)
# For local development SQLite:
DATABASE_URL="file:./db/custom.db"

# For Vercel production:
# If left unset or set to file:./db/custom.db, the app will automatically use the bundled SQLite database in /tmp.
# If you configure a hosted PostgreSQL database (e.g. Neon, Supabase), set:
# DATABASE_URL="postgresql://user:password@ep-xyz.neon.tech/neondb?sslmode=require"

# NextAuth configuration
NEXTAUTH_SECRET="qanoonpk-super-secret-key-2026-production"
NEXTAUTH_URL="https://qanoon-pk-pakistan-legal-directory.vercel.app"

# Default Administrator credentials
ADMIN_EMAIL="admin@qanoon.pk"
ADMIN_PASSWORD="Admin@123"
